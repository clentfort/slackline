{
  "name": "slackline",
  "version": "0.1.0",
  "description": "CLI automation for Slack via Playwright",
  "type": "module",
  "bin": {
    "slackline": "dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "playwright:install": "playwright install chrome",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.55.1",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/yargs": "^17.0.33",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^4.0.18"
  }
}
